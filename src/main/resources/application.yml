spring:
  profiles:
    active: dev
  servlet:
    multipart:
      #设置单个文件大小
      max-file-size: 10MB
      # 设置单次请求文件的总大小
      max-request-size: 50MB

server:
  port: 8088


#showSql
#logging:
#  level:
#    com:
#      needayeah:
#          elastic:
#            mapper: debug